apiVersion: v2
name: fyers-collector
description: >
  The merged Fyers collector — ONE always-on container that captures per-minute
  snapshots (options chains, futures, spot, VIX, macro universe) AND owns the
  daily OAuth click-flow login (/callback + /healthz on the Fyers-registered
  port) over a single shared PVC. Merge landed 2026-09-23.
type: application
version: 0.1.0
appVersion: "1.0.0"
